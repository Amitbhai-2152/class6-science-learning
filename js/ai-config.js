// AI Tutor endpoint configuration.
// Keep this file free of secrets. The OpenAI API key belongs only on the backend.
window.CLASS6_AI_CONFIG = window.CLASS6_AI_CONFIG || {
  // Same-origin default for a combined frontend + backend deployment.
  // For GitHub Pages + separate backend, replace this with your deployed backend URL + /api/tutor.
  endpoint: "/api/tutor"
};
