(function(){
'use strict';
function mathsStats(){try{const h=JSON.parse(localStorage.getItem('mathsExamHistory')||'[]');if(!Array.isArray(h)||!h.length)return {pct:0,xp:0};const total=h.reduce((s,x)=>s+Number(x.total||0),0),score=h.reduce((s,x)=>s+Number(x.score||0),0),pct=total?Math.round(score/total*100):0,xp=h.reduce((s,x)=>s+10+(Number(x.score)||0)*2,0);return {pct,xp}}catch(_){return {pct:0,xp:0}}}
function hindiStats(){try{const h=JSON.parse(localStorage.getItem('class6HindiProgressV2')||'null');if(!h)return {pct:0,xp:0,badges:0,streak:0};const topics=Object.values(h.topics||{}),pct=topics.length?Math.round(topics.reduce((a,b)=>a+Number(b||0),0)/topics.length):Number(h.best||0);return {pct,xp:Number(h.xp)||0,badges:(h.badges||[]).length,streak:Number(h.streak)||0}}catch(_){return {pct:0,xp:0,badges:0,streak:0}}}
function sstStats(){try{const h=JSON.parse(localStorage.getItem('socialScienceProgressV3')||'null');if(!h)return {pct:0,xp:0,badges:0,streak:0};const cs=Object.values(h.chapters||{}),pct=cs.length?Math.round(cs.reduce((s,c)=>s+Number(c.progress||0),0)/14):0;return {pct,xp:Number(h.xp)||0,badges:Array.isArray(h.badges)?h.badges.length:0,streak:Number(h.streak)||0}}catch(_){return {pct:0,xp:0,badges:0,streak:0}}}
function scienceStats(){try{const h=JSON.parse(localStorage.getItem('class6ScienceProgressV9')||'{}'),completed=Array.isArray(h.completed)?h.completed.length:0;return {xp:Number(h.xp)||0,badges:Array.isArray(h.badges)?h.badges.length:0,streak:Number(h.streak)||0,pct:Math.round((Number(h.progress)||0))}}catch(_){return {xp:0,badges:0,streak:0,pct:0}}}
function sync(){const m=mathsStats(),h=hindiStats(),ss=sstStats(),sci=scienceStats();const pct=document.getElementById('homeMathsPct'),bar=document.getElementById('homeMathsBar');if(pct)pct.textContent=m.pct+'%';if(bar)bar.style.width=m.pct+'%';let row=document.getElementById('homeHindiProgressRow');if(!row){const anchor=document.getElementById('homeMathsBar')?.closest('.home-bar-row');if(anchor){row=document.createElement('div');row.id='homeHindiProgressRow';row.className='home-bar-row';row.innerHTML='<div class="home-bar-top"><span>🪔 Hindi</span><span id="homeHindiPct">0%</span></div><div class="home-bar"><i id="homeHindiBar"></i></div>';anchor.after(row)}}const hp=document.getElementById('homeHindiPct'),hb=document.getElementById('homeHindiBar');if(hp)hp.textContent=h.pct+'%';if(hb)hb.style.width=h.pct+'%';let srow=document.getElementById('homeSstProgressRow');if(!srow){const anchor=document.getElementById('homeHindiProgressRow')||document.getElementById('homeMathsBar')?.closest('.home-bar-row');if(anchor){srow=document.createElement('div');srow.id='homeSstProgressRow';srow.className='home-bar-row';srow.innerHTML='<div class="home-bar-top"><span>🌏 Social Science</span><span id="homeSstPct">0%</span></div><div class="home-bar"><i id="homeSstBar"></i></div>';anchor.after(srow)}}const sp=document.getElementById('homeSstPct'),sb=document.getElementById('homeSstBar');if(sp)sp.textContent=ss.pct+'%';if(sb)sb.style.width=ss.pct+'%';const total=document.getElementById('homeTotalXP');if(total)total.textContent=sci.xp+m.xp+h.xp+ss.xp;const streak=document.getElementById('homeStreak');if(streak)streak.textContent=Math.max(sci.streak,h.streak,ss.streak);const badge=document.getElementById('badgeHome');if(badge)badge.textContent=sci.badges+h.badges+ss.badges}
window.HomeProgressSync={refresh:sync};window.addEventListener('DOMContentLoaded',()=>setTimeout(sync,50));window.addEventListener('storage',sync);window.addEventListener('hindi:progress',sync);setInterval(()=>{if(document.visibilityState==='visible')sync()},2000);
})();
